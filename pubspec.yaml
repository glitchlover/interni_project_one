name: interni_project_one
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  faker: ^2.1.0
  flutter:
    sdk: flutter
  get: ^4.6.6
  path_provider: ^2.1.3
  path_to_regexp: ^0.4.0
  sqflite: ^2.3.3+1
  sqflite_common_ffi: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
